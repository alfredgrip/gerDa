% $Id: dsekbrev.sty,v 1.3 2001/12/16 01:17:12 magnus Exp $

\RequirePackage{dsekcommon}

\typeout{This is dsekbrev.sty, version 2001-12-16}%

\pagestyle{fancy}

\lfoot{\parbox{\textwidth}{%
    \footnotesize D-Sektionen inom TLTH $\cdot$
    Box 118, 221~00 Lund $\cdot$
    tel: 046--139473 $\cdot$
    fax: 046--139473 $\cdot$
    e-post: info@dsek.lth.se}}
\cfoot{}
\rfoot{}

% \fromaddress{TEXT}
%
% Adds a nicely formatted sender address. Use at the top of the letter.
\newcommand{\fromaddress}[1]{\vspace{10mm}#1}
% END (fromaddress)

% \toaddress{TEXT}
%
% Adds a recipient address to the right of the p. Use this a the top of the
% letter, preferably after a \fromaddress command.
\newcommand{\toaddress}[1]{
  \vspace{3mm}
  \hspace{90mm}\parbox{60mm}{#1}

  \vspace{15mm}}
% END (toaddress)

% \header{UTSKOTT}{DATUM}
%
% Adds a page header with the subtitle UTSKOTT and dated DATUM.
\newcommand{\header}[2]{\vspace{-55mm}
  \noindent\parbox{22mm}{\Dlogosmall[16mm]}\parbox{117mm}{\raggedright
  \textbf{DATATEKNIKSEKTIONEN INOM TLTH}\newline
  \small{#1}}
\hspace{2mm}\parbox{30mm}{#2\newline~}}
% END (header)
